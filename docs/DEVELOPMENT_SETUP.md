# Development Setup

1. Install Python 3.12+
2. Install dependencies: `pip install reportlab streamlit`
3. Copy `.env.example` to `.env` and fill out your keys.
4. Run CLI: `python backend/services/orchestrator_service.py run test_response.json`
5. Run Frontend: `streamlit run frontend/app.py`
